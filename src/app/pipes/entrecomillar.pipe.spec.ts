import { EntrecomillarPipe } from './entrecomillar.pipe';

describe('EntrecomillarPipe', () => {
  it('create an instance', () => {
    const pipe = new EntrecomillarPipe();
    expect(pipe).toBeTruthy();
  });
});
